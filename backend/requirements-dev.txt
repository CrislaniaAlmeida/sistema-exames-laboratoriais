# Dependencias extras para rodar os testes localmente (nao sao
# necessarias em producao). Instale com:
#   pip install -r requirements.txt -r requirements-dev.txt
# e depois rode os testes com:
#   pytest -v
pytest==9.1.1
